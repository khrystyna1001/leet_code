class Solution:
    def hammingWeight(self, n: int) -> int:
        
        count = 0
        while n:
            n &= (n - 1) # This drops the lowest '1' bit
            count += 1
        return count
